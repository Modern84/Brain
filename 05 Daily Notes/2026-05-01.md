---
tags: [tagesbuch]
date: 2026-05-01
---

## Jarvis Phase 1 — fertig (01:45)

**Was lief:**
- Skelett gebaut (Wake-Word openWakeWord, Whisper-STT lokal, Claude Sonnet 4.6, ElevenLabs TTS)
- Hardening: System-Prompt, Rate-Limit, Error-Handling, JSONL-Logging
- macOS CoreAudio Race-Condition gefixt
- ElevenLabs Free-Tier wegen Tailscale-VPN-Detection gesperrt → Starter Plan ($5/Monat) gelöst
- Piper TTS lokal als Fallback installiert (de_DE-thorsten-high, 109 MB, gitignored)
- Daniel-Stimme aktiv, deutscher Briefing-Output funktioniert

**Commits (5):**
- `4ddd8fd` Phase 1 Solo Mac, Read-Only Skelett
- `493e85f` Phase 1 Hardening
- `e7f154f` Revert model to claude-sonnet-4-6
- `0592619` macOS CoreAudio Race-Condition Fix
- `35568ad` elevenlabs working with piper fallback

**Latenz-Messung (Live-Lauf 02:11):**
- Wake → Transkript: 0.69 s
- Transkript → Response-Text: 7 s
- Total (Wake → Audio fertig): **71.33 s**
- Bottleneck: Response-Audio-Wiedergabe + Cold-Start ElevenLabs. Phase 1.1 (Streaming) zielt auf < 3 s.

**Cost:**
- $5/Monat Anthropic (API)
- $5/Monat ElevenLabs Starter (30k Zeichen)
- = **$10/Monat fix für Jarvis**

**Apple-Max-Abo gekündigt** — läuft 7. Mai 2026 aus, danach Browser-Abo $100. Reduktion vs. Max-Plan.

**Offen (Phase 1.2–1.4):** VAD statt 4s-Fenster, Tool-Wrapper für Phase 2, Wake-Debounce. Siehe TASKS.md Zeilen 308–313.

## Jarvis Phase 1.1 — Streaming fertig (02:20)

**Was geändert:**
- `tts.py`: `speak_stream(text_iter)` mit Satz-Splitter (`.!?…`) und Player-Thread (queue.Queue + afplay)
- `jarvis.py`: `client.messages.stream()` ersetzt `messages.create()`; Generator gibt Text-Chunks direkt an `tts.speak_stream`
- Latenz-Logging: `first_audio` (ms nach Sprachende) + `turn_complete` (Wake → Audio fertig)

**Latenz-Vergleich:**
| Metrik | Phase 1 (sequentiell) | Phase 1.1 (Streaming) | Δ |
|---|---|---|---|
| first_audio nach Sprachende | ~30+ s | **3.446 s** | −89 % |
| turn_complete (gesamt) | 71.33 s | 55.04 s | −23 % |

3-s-Akzeptanzkriterium um 446 ms verfehlt — akzeptiert (Optimierungs-Trades zu teuer: Komma-Trigger schadet Prosodie, Kontext-Kürzung schadet Briefing-Qualität, Whisper-tiny erhöht STT-Fehlerrate).

**Bug entdeckt:** Doppelter Wake-Trigger (zwei identische `wake`+`transcript`-Events bei 02:20:32). Als Phase 1.4 in TASKS.md.

## Jarvis Phase 1.2 + 1.4 — VAD + Debounce (02:35)

**Was geändert:**
- `wake.py`: `record_until_silence()` ersetzt `record_utterance()` — webrtcvad-wheels (Aggressiveness 2, 30-ms-Frames, 800-ms-Silence-Stop, 500-ms-Min, 15-s-Max). Liefert `(audio, reason, duration_ms)` mit `reason ∈ {"silence", "max_timeout"}`.
- `wake.py listen_for_wake(debounce_ms=500)`: nach Detection drained 500 ms aus dem Stream, dann return.
- `jarvis.py`: ein-Zeilen-Wechsel + neuer Log-Event `recording_stopped`.
- `requirements.txt`: webrtcvad-wheels + piper-tts ergänzt.

**Lib-Wahl webrtcvad-wheels** statt webrtcvad: reine Wheel auf macOS arm64 ohne C-Build-Tools. Funktional identisch.

**Test-Ergebnisse:**
| Test | recording_stopped | Aufnahme-Dauer | first_audio |
|---|---|---|---|
| Kurz „briefing" | silence | 1.6 s | **2823 ms** ✅ unter 3 s |
| Lang „lies mir die Top 3 Tasks…" | silence | 4.0 s | 3266 ms |

VAD funktioniert sauber, keine Truncation bei langer Anweisung. **Bonus:** bei kurzer Aufnahme schaffen wir das Phase-1.1-3-s-Ziel.

**Doppel-Wake-Bug (Phase 1.1):** war vermutlich Multi-Prozess-Log-Artefakt — drei Zombie-Jarvis-Instanzen liefen parallel und loggten alle Events doppelt/dreifach in dieselbe JSONL. Debounce-Code ist trotzdem defensiv drin. Neuer Task **Phase 1.5 — Single-Instance-Lock** ergänzt, um das Multi-Prozess-Problem strukturell zu lösen.

## Jarvis Phase 1.5 — Single-Instance-Lock (02:50)

PID-File `~/jarvis/jarvis.pid`: tote PID wird überschrieben, lebendige PID → exit 1 mit Hinweis. atexit + SIGTERM/SIGINT-Handler räumen auf. Tests bestanden (tote/lebendige PID).

Löst das Multi-Prozess-Problem strukturell, das uns heute Tests verfälscht hat. Commit `8f82676`.

**Heute Nacht insgesamt — 6 Commits:**
- `4ddd8fd` Phase 1 Skelett · `493e85f` Hardening · `e7f154f` Modell-Revert · `0592619` CoreAudio-Fix · `35568ad` ElevenLabs+Piper · `e3ad9da` Phase 1 docs
- `5a65f39` Phase 1.1 Streaming · `3c8ab4e` Phase 1.2+1.4 VAD+Debounce · `8f82676` Phase 1.5 Single-Instance

Phase 1.3 (Tool-Wrapper) bleibt offen — größerer Refactor, besser ausgeschlafen.

**Verknüpfungen:**
- [[02 Projekte/Jarvis - Sprachassistent]]
- `~/jarvis/README.md`

## Notizen

- 19:56: - Jarvis Briefing abgerufen.
- 19:42: Tagebuch-Schreibtest Phase 2.1 formal bestätigt — Jarvis append_daily funktioniert live.
- 16:18: Sebastian freut sich, dass Jarvis stetig an der Weiterentwicklung interessiert bleibt und dranbleibt.
- 11:57: Sebastian hat sich gefreut, dass Jarvis selbstständig seine Aufgabe erkannt hat, und wünscht sich, dass Jarvis stetig an seiner eigenen Verbesserung interessiert bleibt.
- 11:04: Jarvis Briefing abgerufen.
- 03:40: Jarvis Phase 2.1 — Schreibrechte ins Brain verifiziert. Tool-Calls (write_inbox, append_daily, add_task) funktionieren live.

## Pilot Fusion-Drawing-Architektur (Schriftfeld L7) — gestartet

**Anlass:** `build_a3.py`-Output verfehlt 30€-Ingenieurbüro-Niveau. Architektur-Pivot: Fusion-Drawing-Workspace als Quelle für Zeichnungen, Patcher nur noch für Schriftfeld-Injection.

**Schritt 1 ✅ — Schriftfeld-Recherche** in [[04 Ressourcen/CAD-Workflows/Schriftfeld DIN ISO 7200 L7]]:
- L7 = Bearbeitet/Konstrukteur, M7 = Geprüft/Ingenieur (Bens-Solid-Edge-Zellkoordinaten, kein ISO-Begriff)
- Layout-Maße aus Fusion-Klon-PDF rekonstruiert; Original-Solid-Edge-`.dft` ist Binär, ohne Reader nicht verifizierbar
- 8 ISO-7200-Pflichtfelder identifiziert; `Date of issue` und `Rev.` fehlen aktuell

**Validierung Mo (STOP-Punkt):**
- L7=`Hartmann`, M7=`Woldrich` (Nachname-only, iProperty-befüllbar)
- `Date of issue` (auto) + `Rev.` (manuell, Start `00`) als Pflichtfelder ins Template
- **Schritt 2 PAUSIERT** bis Reiner ein PDF-Druckbild des Solid-Edge-Originals liefert. Begründung: Bens-Konstruktionsdaten dürfen nicht durch Online-Konverter („Selbst ist Sicherheit. Sicherheit ist Selbst.")
- Wichtige Korrektur: **Reiner arbeitet mit Solid Edge (Siemens), nicht SolidWorks.** Memory-Eintrag aktualisiert.

**Folge-Artefakte:**
- [[02 Projekte/WEC/Onboarding Reiner/Solid Edge Viewer Setup]] — Win-PC-Anleitung für Reiner (Siemens-Viewer install, `.dft` öffnen, A3-PDF drucken, in `WEC-Shared/Bens/` ablegen)
- [[04 Ressourcen/CAD-Workflows/Fusion 360 Add-Ins]] — Sheet Metal DXF Creator (free, empfohlen). BOM Creator nicht free (~10 USD); Entscheidung: Fusion-natives BOM zuerst, Add-In erst bei konkretem Schmerz.

**Offen:**
- Reiner-Setup-Anleitung an Reiner geben (E-Mail / WhatsApp / direkt)
- Auf Original-PDF warten → dann Schritt 2 (Fusion-Template-Klon)
- Sheet Metal DXF Creator installieren (nicht-blockierend)

### Korrektur Pilot — Schritt 2 entblockt

**Vault-Fund:** `03 Bereiche/WEC/raw/Standards WEC/Templates/WEC.dwg` (120 KB) ist direkt in Fusion als Title Block importierbar (Autodesk-Workflow: untere rechte Ecke auf 0,0, ATTDEF-Attribute setzen, dann Sheet Settings → Title Block → New → From DWG).

Reiner-PDF-Umweg über Solid Edge Viewer entfällt für den Pilot. Reiner-Anleitung bleibt als Generalwerkzeug aktiv, aber nicht-blockierend.

**Strategische Klarstellung:** Schritt 2 baut nun das **WEC-eigene Template** (für MThreeD/WEC-direkt-Aufträge). Bens-Klon-Variante mit `Bens_Vordruck.dwg` folgt analog, wenn gebraucht. Das ist konsistent mit der 2026-05-01-Parallel-Strategie (Fusion 360 für eigene Konstruktion, Solid Edge für Bens-Lieferungen).

Anleitung: [[04 Ressourcen/CAD-Workflows/Fusion 360 Drawing Template L7]]. Phasen A–D dokumentiert (DWG-Vorbereitung, Title Block in Fusion, Template speichern, Validierung am Pilot R_Blech_Klein).

## WEC-Strategie-Update — Korpus-Auslesen + KI-Pipeline

Sebastian hat im Diktat-Strom die Vision für WEC ab August scharf formuliert. Eingang in Bestand:

- [[02 Projekte/WEC Neustart mit Reiner/WEC Neustart mit Reiner#Update 2026-05-01 — Strategie scharf formuliert]] — Update-Sektion mit O-Ton-Diktaten (Daten, Rollen, Konstruktion, Output, Kunden) + 5 strategische Prinzipien.
- [[02 Projekte/WEC Neustart mit Reiner/Strategie - Korpus-Auslesen und KI-Pipeline]] — neue Konzept-Notiz: Ausgangslage, Architektur-Frage, drei Pipeline-Optionen (Cloud / lokal Mac Studio Ultra / Hybrid), Klärungs-Liste, nächste Schritte.

**Kern-These:** Sebastian und Reiner allein können Volker-Qualität nur halten, wenn Reiners 30-Jahre-Konstruktions-Korpus per KI als Norm-/Qualitäts-Referenz nutzbar gemacht wird.

**Output-First-Strategie festgehalten:** STEP+DXF+Stückliste+Notizen-PDF statt 2D-Zeichnung. Lokale LLMs als Brückenzeit-Frage offen, Re-Eval mit Mac Studio Ultra. Reiner bleibt aktiv, anti-Computer; Sebastian übernimmt digitales Hintergrund-Management.

## Jarvis Phase 1.5 + 2.1 — live getestet ✅

- Commit `6be3c2c` (Brain) gepusht nach Aufräumen: 141-MB-STEP untracked + Anthropic-Key in `.obsidian/plugins/copilot/data.json` aus Tracking + .gitignore erweitert.
- Live-Test bestanden — Phase 1.5 (Single-Instance-Lock) + Phase 2.1 (Brain-Schreibrechte: `write_inbox`, `append_daily`, `add_task`) abgehakt.
- Folge-Fixes (~/jarvis Commit `578a536`): Wake-Threshold `0.5 → 0.65`, Iron-Man-Startup-Sound (`afplay ~/jarvis/sounds/startup.mp3` async direkt nach Wake-Detection, fehlende Datei = silent skip).

## Anthropic-Key-Rotation (Obsidian-Copilot)

GitHub Secret-Scanning hatte beim Push gemeldet: Anthropic-Key im Klartext in `data.json`. Datei jetzt aus Git, lokal weiter vorhanden.

- Alter Key revoked auf console.anthropic.com.
- Neuer Key „obsidian-copilot" erstellt + in Obsidian eingetragen.
- Beifang: `plusLicenseKey` enthielt versehentlich auch den Anthropic-Key (Fehlplatzierung beim Einfügen) → Feld geleert. Verifikation: `plusLicenseKey` leer, `anthropicApiKey` gesetzt.

## Brain-Pflege-Agent — Pilot verworfen (17:56)

Konzept-Notiz [[02 Projekte/Brain-Pflege-Agent (Managed-Agents-Pilot)]] auf Status `verworfen` gesetzt nach Lesen der Managed-Agents-Docs (overview, environments, agent-setup).

**Befund:** Architektur passt nicht zu lokalem Vault — Environment ist isolierter Cloud-Container ohne Volume-Mount, Filesystem-Zugriff auf Mac nicht vorgesehen. Pilot wäre nur über git-clone-Workaround machbar, ~30 % Lerneffekt für den eigentlichen Ziel-Use-Case.

**Konsequenz:** Managed Agents erst sinnvoll, wenn [[02 Projekte/MThreeD.io/Analyse - Klipper KI-Debug-Tunnel|Klipper-Tunnel]] reif zur Umsetzung ist. Dort passt die Architektur (long-running, multi-tenant, Hosting + Auth-Refresh durch Anthropic) exakt. Klipper-Analyse-Notiz mit „Architektur-Match"-Sektion ergänzt.

## Phase 2.1 — formal verified (19:58)

Vier Tests, alle grün:

| Test | Tool | Methode | Ergebnis |
|---|---|---|---|
| 1 | `write_inbox` | Live-Mic | ✅ `01 Inbox/2026-05-01-1940-wack-sound-auswahl.md` (Whisper hörte „Wake" → „Wack", Tool sauber) |
| 2 | `append_daily` | Live-Mic | ✅ Eintrag 19:42 in `## Notizen` |
| 3 | `add_task` | Direkt-Code | ✅ Eintrag 19:58 in `## Inbox` — Mikro-Versuche scheiterten 3× an Whisper-base-Verhörern („Aufgrabber", „Weg sound") |
| 4 | Pfad-Validator | Direkt-Code | ✅ `_safe_join` blockt `../etc/passwd` und absolute Pfade, `_slug` säubert bösartige `title`-Strings |

**Erkenntnis:** Whisper-base-STT ist für seltene Befehle/Begriffe zu fragil — Router-Pattern fingen „neue aufgabe" nicht ab, sobald „neue" verschluckt wurde oder „Aufgabe" zu „Aufgrabber" wurde. Fix: `router.py` um `\baufgabe\b`, `\btask\b`, `\bto.?do\b` erweitert (1 Zeile, Selbsttest 14/14 grün). Strukturell offen → neuer Inbox-Task „Phase 1.7: LLM-Router prüfen".

**Cost-Cap-Beifang:** `DAILY_CAP=50` in `jarvis.py` heute überschritten (count=53 nach Phase-1-Dev), Counter via `rm /tmp/jarvis_calls` resetted für die Test-Session.

## ~/jarvis als eigenes GitHub-Repo

`gh` via brew installiert (2.92.0), Browser-Auth auf Account `Modern84`. Repo `Modern84/jarvis` (private) angelegt + initial gepusht. `main` trackt `origin/main`. Inhalt vor Push gegen Secrets geprüft: nur Code + `.env.example`-Platzhalter, Logs/Models/PID per `.gitignore` ausgeschlossen.
